import * as React from 'react';

export interface nameProperties {
}

export class name extends React.PureComponent<nameProperties> {

  constructor(p: nameProperties) {
    super(p);
  }

  render(): React.ReactNode {
    return (
      <div>
      </div>
    );
  }
}